spring:
  datasource:
    url: jdbc:mysql://localhost:3306/server_manager_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: 'create-drop'
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true
